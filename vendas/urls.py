from django.urls import path
from . import views

# Este arquivo ainda não terá muitas rotas, vamos adicionar uma view de teste.

urlpatterns = [
    # path('', views.home, name='home'), # Exemplo que podemos ativar depois
]